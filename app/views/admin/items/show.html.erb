<h1>商品詳細ページ</h1>

<div>
  <table>
    <thead>
      <tr>
        <th>商品イメージ</th>
        <th>商品名</th>
        <th>税抜価格(円)</th>
        <th>商品説明</th>
        <th>登録日時</th>
        <th>更新日時</th>
      </tr>
    </thead>
    <tbody>
        <tr>
          <td><%= attachment_image_tag @item, :image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %></td>
          <td><%= @item.name %></td>
          <td><%= @item.price %></td>
          <td><%= @item.introduction %></td>
          <td><%= @item.created_at %></td>
          <td><%= @item.updated_at %></td>
        </tr>
    </tbody>
  </table>
</div>

<button>
  <%= link_to "商品情報編集", edit_admin_item_path(@item) %>
</button>