<%= render 'public/public', customer:@customer %>

<div>
  <div>
    <%= attachment_image_tag @item, :image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %></td>
  </div>
  <div>
    <%= @item.name %>
  </div>
  <div>
    <%= @item.introduction %>
  </div>
  <div>
    ¥<%= (@item.price*1.1).to_i %>(税込)
  </div>
</div>

<!--後でメンターに聞く-->
<!--<%= form_with model: @item, local:true do |f| %>-->
<!--  <%= f.select :amount, [[], include_blank: "個数選択" %>-->
<!--<% end %>-->

<div>
  <%= link_to "カートに入れる", cart_items_path %>
</div>